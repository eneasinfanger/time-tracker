can you create a simple single page website that has a table to add a start and end time and a text field to enter a description of an activity. when typing an activity, it can be any text but other matching activities should be suggested so they can be selected. the same thing with the time: it can be any time but the last end time should be suggested on the next start time (1:00 -> 2:00, 2:00 -> 3:00). there should be the possibility to switch between days and add seperate activies for each day, but still suggesting activities from other days. THEN there should be a button which calculates the time each activity took on each day (adding up entries for duplicate activities, only calculate for the current visible day). then there should also be a way to add just text instead of an activity with time, which is ignored when calculating the time but still be displayed in the table. The user should be able to add and remove abilites (and text) between the lines.

The data should be stored in the local storage.

using:
- NO FRAMEWORK
- TAILWIND CSS (is this a framework ? idk but use it)
- STANDARD HTML
- JAVASCRIPT
- NORMAL CSS (if neccesary)
You can (and should) use different (javascript) files for different functionalities.

The design:
- i don't really care about the color scheme
- make it look modern & simple
